test network partitions

test change of leader

change heartbeat timer so it's less complex

needs better msg parsing in split_msg

leader should not commit messages from previous terms

leader log repair
when going back index's if a majority aren't at match index